#ifndef INCLUDE_PANDIGITAL_HPP
#define INCLUDE_PANDIGITAL_HPP

bool pandigital_start(std::vector<unsigned>& n);
bool pandigital_end(std::vector<unsigned>& n);
bool pandigital_dual(std::vector<unsigned>& n);

#endif